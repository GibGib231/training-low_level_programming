#include<stdio.h>
int main(void)
        /* this is a alphabet code thatis to print only  lower case letter */
{
        char lower_case = 'a';
         while(lower_case <= 'z'){
                        putchar(lower_case),
                                lower_case++;
         }
         

                        putchar('\n');

                                return 0;
}
